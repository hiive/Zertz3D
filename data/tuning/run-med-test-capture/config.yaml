# MCTS Hyperparameter Tuning Configuration for run-1
# ===================================================

mode: grid

game:
  rings: 37
  iterations: 2500  # quick test
  games_per_config: 100
  seed: 42  # Unknown - using default
  num_processes: 10  # Unknown - using default

hyperparameters:
  exploration_constant:
    type: values
    values: [0.2, 0.35, 0.5, 1.0, 1.41, 2.0, 3.0]

  fpu_reduction:
    type: values
    values: [null, 0.1, 0.2, 0.3, 0.5]

  max_simulation_depth:
    type: fixed
    value: null

  widening_constant:
    type: values
    values: [null, 8.0, 12.0, 16.0, 20.0]

  rave_constant:
    type: fixed
    value: null

output:
  directory: null
  filename: tuning_results.json

display:
  top_n: 10
  verbose: true